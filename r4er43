ProcessBuilder pb = new ProcessBuilder("qvoronoi","o","TO output","< 9Puntos");
		pb.redirectErrorStream(true);
		
		try{
			Process shell = pb.start();
			 
		}catch (IOException e) {
			System.out.println("Error occured while executing Linux command. Error Description: " + e.getMessage());
		}
		
		qvoronoi TO output < input
		
		
		       
        
        /*Dibujado con lineas
        for (int i = 0; i < nPoints - 1; i++) {
        	g2d.drawLine(points[i].getX(), points[i].getY(), points[i+1].getX(), points[i+1].getY());
        }
        Cerrar la figura
        g2d.drawLine(points[0].getX(), points[0].getY(), points[nPoints-1].getX(), points[nPoints-1].getY());*/